<% if current_user %>
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="#"><%= Preference.where(key: 'title').first %></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item <%= is_active_page? root_path %>">
          <a class="nav-link" href="#"><%= t('navbar.startpage') %></a>
        </li>
      </ul>

      <ul class="navbar-nav ml-auto">
        <li><%= button_to t('navbar.logout'), destroy_user_session_path, method: 'delete', class: "btn btn-danger ml-auto" %></li>
      </ul>
    </div>
  </nav>
<% end %>
